name: one_thought_app
description: One Thought Initiatives App

publish_to: none

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.0.0
  firebase_auth: ^4.0.0
  cloud_firestore: ^4.0.0
  http: ^0.13.5

flutter:
  uses-material-design: true
